name: Android CI
name: Android CI.yml

on:
  push:
@@ -23,4 +23,4 @@ jobs:
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build
      run: ./gradlew build